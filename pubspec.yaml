name: nsfw_detector_flutter
description: A Flutter package to detect NSFW (Not Safe For Work / NUDE / adults) content and classify SAFE content without requiring assets. Easily integrate NSFW detection into your app.
version: 1.1.0
homepage: https://github.com/hoyaaaa/nsfw_detector_flutter

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cross_file: ^0.3.4+2
  tflite_flutter: ">=0.10.4 <1.0.0"
  image: ">=4.2.0 <5.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/nsfw.tflite

platforms:
  android:
  ios:
